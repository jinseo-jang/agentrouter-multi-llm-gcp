apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rbac.yaml
  - job.yaml

configMapGenerator:
  - name: e2e-benchmark-cm
    files:
      - benchmark.py
      - prompts.json
